---
import { reviews } from '../data/content';
import { site } from '../data/site';
---
<section class="block" id="reviews">
  <div class="wrap">
    <div class="sec-head reveal">
      <p class="eyebrow">Real local reviews</p>
      <h2>Geelong homeowners love working with Jackson</h2>
      <p><span class="stars">★★★★★</span> Rated {site.ratingValue} from {site.reviewCount} reviews across Google &amp; Facebook</p>
    </div>
    <div class="tgrid">
      {reviews.map((r) => (
        <div class="tcard reveal">
          <div class="stars">{'★'.repeat(r.stars)}</div>
          <blockquote>"{r.quote}"</blockquote>
          <div class="who"><div class="av">{r.initials}</div><div><b>{r.name}</b><span>{r.suburb}</span></div></div>
        </div>
      ))}
    </div>
  </div>
</section>
<!-- Quotes below are placeholder layout content — swap for Jackson's real Google/Facebook reviews (real names on file: Aneliz, Alen, Jeremey, Gemma, Angelo, Emily, Bianca, Cheryl, Olivia) once pulled from the legacy site. -->
